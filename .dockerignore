.git
.github
.githooks
node_modules
dist
coverage

# The desktop app never ships in server images; only its manifest is needed
# so `npm ci --workspaces=false` can validate the workspace-aware lockfile.
app/**
!app/package.json

.env
.env.*
!.env.example

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

data/**
!data/
!data/skills/
!data/skills/**

// Runtime compatibility adapter. The application implementation lives in the
// shared Discord layer so both generated-code and interaction adapters can use
// the same mutation and reminder lifecycle.
export * from "../shared/todo-application";
